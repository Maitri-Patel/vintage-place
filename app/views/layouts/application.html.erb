<!DOCTYPE html>
<html>
  <head>
    <title>VintagePlace</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS (latest version) -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" integrity="sha384-eOJFj2Hod+2LF5/pttvA+WeomMSmWT8fvO8F9B0jT9E+3pVHUsHn/P8tL3MVd0ab" crossorigin="anonymous">
    
    <!-- Rails Stylesheets -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>

    
    <!-- JavaScript Importmap Tags -->
    <%= javascript_importmap_tags %>
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
</head>

  <body>
   <nav>
  <div class="container">
    <%= link_to 'Vintage Place', root_path, class: 'brand' %>

    <div class="navbar" id="navbarContent">
      <ul class="nav">
        <% Category.all.each do |category| %>
          <li class="item">
            <%= link_to category.name, products_path(category_id: category.id), class: 'link' %>
          </li>
        <% end %>
      </ul>
      <form class="search-form">
        <input type="search" placeholder="Search" aria-label="Search">
        <button type="submit">Search</button>
      </form>
      <ul class="nav">
        <li class="item">
          <%= link_to raw('<i class="cart-icon"></i> Cart') %>
        </li>
        <li class="item">
          <%= link_to raw('<i class="login-icon"></i> Login') %>
        </li>
        <li class="item">
          <%= link_to raw('<i class="signup-icon"></i> Sign Up') %>
        </li>
      </ul>
    </div>
  </div>
</nav>
    <%= yield %>
  </body>
</html>

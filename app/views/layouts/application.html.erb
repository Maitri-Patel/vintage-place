<!DOCTYPE html>
<html>
  <head>
    <title>VintagePlace</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css">
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to 'Vintage Place', root_path, class: 'navbar-brand' %>

    <div class="collapse navbar-collapse" id="navbarContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% Category.all.each do |category| %>
          <li class="nav-item">
            <%= link_to category.name, products_path(category_id: category.id), class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
      <form class="d-flex me-auto">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to raw('<i class="bi bi-cart3"></i> Cart')%>
        </li>
        <li class="nav-item">
          <%= link_to raw('<i class="bi bi-person"></i> Login')%>
        </li>
        <li class="nav-item">
          <%= link_to raw('<i class="bi bi-person-plus"></i> Sign Up') %>
        </li>
      </ul>
    </div>
  </div>
</nav>

    <%= yield %>
  </body>
</html>
